export default function RoundedButton({ children }) {
  return (
    <button
      className={''}
      // onMouseLeave={}
      // onMouseEnter={}
    >
      {children}
      <span />
    </button>
  );
}
